% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Texto.R
\name{LimpiarCadena}
\alias{LimpiarCadena}
\title{Limpiar y Normalizar una Cadena de Texto}
\usage{
LimpiarCadena(
  x,
  rem_espacios = FALSE,
  rem_numeros = TRUE,
  rem_caresp = TRUE,
  rem_acentos = TRUE
)
}
\arguments{
\item{x}{Un vector de caracteres con las cadenas de texto a limpiar.}

\item{rem_espacios}{Un valor lógico (TRUE o FALSE) que indica si se deben eliminar todos los espacios en blanco.
El valor predeterminado es \code{FALSE}.}

\item{rem_numeros}{Un valor lógico que indica si se deben eliminar los números.
El valor predeterminado es \code{TRUE}.}

\item{rem_caresp}{Un valor lógico que indica si se deben eliminar los caracteres especiales (no alfanuméricos).
El valor predeterminado es \code{TRUE}.}

\item{rem_acentos}{Un valor lógico que indica si se deben eliminar los acentos.
El valor predeterminado es \code{TRUE}.}
}
\value{
Un vector de caracteres con las cadenas de texto limpias, después de aplicar las transformaciones especificadas.
}
\description{
Esta función limpia y normaliza una cadena o un vector de texto aplicando diferentes transformaciones,
tales como la eliminación de espacios, números, caracteres especiales y acentos, según se especifique.
Los valores `NA` se conservan en sus posiciones originales.
}
\examples{
LimpiarCadena(c("Â¡Hola, mundo!", "Texto con números 123 y acentos áéíóú."))
# Devuelve: "HOLA MUNDO" "TEXTO CON NUMEROS Y ACENTOS AEIOU"
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Inputs.R
\name{ListaDesplegable}
\alias{ListaDesplegable}
\title{Lista Desplegable con Búsqueda y Selección Múltiple}
\usage{
ListaDesplegable(
  inputId,
  label = NULL,
  choices,
  selected = choices,
  multiple = TRUE,
  fem = FALSE,
  ns = NULL
)
}
\arguments{
\item{inputId}{Cadena de caracteres. ID único del input para identificarlo en el servidor.}

\item{label}{Cadena de caracteres o NULL. Etiqueta que se mostrará encima del input.
Si es NULL, no se mostrará etiqueta.}

\item{choices}{Vector nombrado o lista. Opciones disponibles para seleccionar.
Puede ser un vector de caracteres o una lista nombrada.}

\item{selected}{Vector. Opciones que estarán seleccionadas por defecto.
Por defecto selecciona todas las opciones disponibles.}

\item{multiple}{Lógico. Si TRUE permite selección múltiple, si FALSE solo una opción.
Por defecto es TRUE.}

\item{fem}{Lógico. Si TRUE usa terminología femenina ("Todas", "Ninguna"),
si FALSE usa terminología masculina ("Todos", "Ninguno"). Por defecto es FALSE.}

\item{ns}{Función de namespace o NULL. Función de namespace para módulos de Shiny.
Si se proporciona, se aplicará al inputId automáticamente.}
}
\value{
Un objeto tagList de Shiny que contiene el CSS personalizado y el
  pickerInput configurado con todas las opciones especificadas.
}
\description{
Crea un input de selección múltiple personalizado con funcionalidades
  de búsqueda en vivo, selección/deselección masiva y estilos CSS personalizados.
  Incluye soporte para módulos de Shiny y textos dinámicos según género.
}
\details{
Lista Desplegable Personalizada
}
\examples{
# Ejemplo básico con opciones de texto
ListaDesplegable(
  inputId = "mi_selector",
  label = "Selecciona opciones:",
  choices = c("Opción 1", "Opción 2", "Opción 3")
)

# Ejemplo con terminología femenina
ListaDesplegable(
  inputId = "categorias",
  label = "Categorías:",
  choices = c("Categoría A", "Categoría B", "Categoría C"),
  fem = TRUE
)

# Ejemplo para uso en módulos
# En el UI del módulo:
ListaDesplegable(
  inputId = "selector_modulo",
  label = "Opciones del módulo:",
  choices = c("A", "B", "C"),
  ns = ns
)

# Ejemplo con selección única
ListaDesplegable(
  inputId = "unica_opcion",
  label = "Selecciona una opción:",
  choices = c("Solo A", "Solo B", "Solo C"),
  selected = "Solo A",
  multiple = FALSE
)

}

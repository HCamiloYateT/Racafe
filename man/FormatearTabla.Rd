% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Formatos.R
\name{FormatearTabla}
\alias{FormatearTabla}
\title{Formatear tabla numérica con defaults globales y overrides por fila}
\usage{
FormatearTabla(
  data,
  filas_fmt = list(),
  formato_def = "numero",
  negrita_def = FALSE,
  color_def = "#000000",
  meta_def = NA,
  prop_def = TRUE
)
}
\arguments{
\item{data}{\code{data.frame} o \code{tibble} con al menos una columna.}

\item{filas_fmt}{Lista de listas con overrides por fila. Cada elemento puede
incluir: \code{formato}, \code{negrita}, \code{color}, \code{meta} y \code{prop}.
Los nombres de \code{filas_fmt} pueden corresponder al valor de la primera
columna de \code{data} o al índice de fila como cadena.}

\item{formato_def}{Formato numérico global por defecto (ver \code{\link[=DefinirFormato]{DefinirFormato()}}).}

\item{negrita_def}{Lógico global por defecto para negrita.}

\item{color_def}{Color global por defecto en formato hexadecimal.}

\item{meta_def}{Meta global por defecto para coloreo condicional.}

\item{prop_def}{Lógico global por defecto para dirección de color según meta.}
}
\value{
Objeto del mismo tipo que \code{data}, con las columnas numéricas convertidas a
HTML formateado por fila.
}
\description{
Aplica \code{\link[=FormatearNumero]{FormatearNumero()}} a todas las columnas numéricas de una tabla,
permitiendo definir parámetros globales (\code{*_def}) y sobrescribirlos por fila
usando \code{filas_fmt}.
}
\details{
Los overrides se resuelven con la siguiente prioridad:
\enumerate{
\item por valor de la primera columna (por ejemplo, \code{Item}),
\item por posición de fila como texto (\code{"1"}, \code{"2"}, ...).
}
}
\examples{
datos <- data.frame(
  Item = c("Ventas", "Margen", "Meta"),
  Ene = c(1000, 0.34, 1200),
  Feb = c(950, 0.31, 1200)
)

FormatearTabla(
  data = datos,
  formato_def = "numero",
  negrita_def = FALSE,
  filas_fmt = list(
    Ventas = list(formato = "dinero", negrita = TRUE, meta = 980, prop = TRUE),
    Margen = list(formato = "porcentaje", color = "#0B5345"),
    "3"    = list(formato = "dinero", color = "#1F618D")
  )
)
}
\references{
\code{\link[=FormatearNumero]{FormatearNumero()}} y \code{\link[=DefinirFormato]{DefinirFormato()}} para formatos y estilos disponibles.
}

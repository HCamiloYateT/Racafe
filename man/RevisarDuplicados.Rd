% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Utilidades.R
\name{RevisarDuplicados}
\alias{RevisarDuplicados}
\title{Revisar llaves duplicadas entre dos tablas}
\usage{
RevisarDuplicados(x, y, by)
}
\arguments{
\item{x, y}{Data.frames a evaluar.}

\item{by}{Vector de caracteres con los nombres de las columnas llave o un
vector nombrado en el formato \code{col_x = col_y} cuando las llaves difieren.}
}
\value{
Una lista con cuatro elementos: \code{x_tiene_duplicados} y
\code{y_tiene_duplicados} (valores l\'ogicos) y \code{duplicados_x} y \code{duplicados_y}
(data.frames con las filas duplicadas en cada tabla).
}
\description{
Eval\'ua si dos data.frames tienen filas duplicadas seg\'un un conjunto de llaves
y devuelve el detalle de las filas repetidas en cada tabla. Admite llaves con
nombres id\'enticos o mapeos distintos entre \code{x} y \code{y} siguiendo la sintaxis de
\code{dplyr}.
}
\examples{
library(dplyr)

datos_a <- tibble::tibble(id = c(1, 1, 2), valor = c("a", "a", "b"))
datos_b <- tibble::tibble(id = c(1, 2, 2), valor_extra = c("x", "y", "y"))

RevisarDuplicados(datos_a, datos_b, by = "id")

# Llaves con nombres distintos
datos_c <- tibble::tibble(id_local = c(10, 10), valor = c("A", "B"))
datos_d <- tibble::tibble(id_remoto = c(10, 20), valor = c("C", "D"))
RevisarDuplicados(datos_c, datos_d, by = c(id_local = "id_remoto"))
}
\references{
Wickham H., Fran\,{c}ois R., Henry L. \& M\"{u}ller K. (2023). dplyr: A Grammar of Data Manipulation. R package version 1.1.4.
}

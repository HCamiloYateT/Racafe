% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Inputs.R
\name{BotonesRadiales}
\alias{BotonesRadiales}
\title{Botones radiales estilizados}
\usage{
BotonesRadiales(inputId, label = NULL, choices, selected = NULL,
  usar_iconos = FALSE, iconos = NULL, tooltips = NULL, ns = NULL)
}
\arguments{
\item{inputId}{Cadena de caracteres. Identificador único del input.}

\item{label}{Cadena de caracteres o \code{NULL}. Etiqueta que se mostrará junto al
grupo de botones.}

\item{choices}{Vector con las opciones disponibles. Puede ser nombrado para
mostrar etiquetas distintas a los valores enviados al servidor.}

\item{selected}{Valor inicialmente seleccionado. Debe coincidir con uno de
los elementos de \code{choices}.}

\item{usar_iconos}{Lógico. Si es \code{TRUE} utiliza los nombres de iconos de
Font Awesome especificados en \code{iconos}.}

\item{iconos}{Vector de caracteres con los nombres de los iconos a usar. Debe
tener la misma longitud que \code{choices} cuando \code{usar_iconos = TRUE}.}

\item{tooltips}{Vector de caracteres con los textos de ayuda que se mostrarán
como tooltips al pasar el cursor sobre cada botón. Debe tener la misma
longitud que \code{choices}.}

\item{ns}{Función de namespace o \code{NULL}. Se utiliza para adaptar el
identificador cuando el input se usa dentro de módulos.}
}
\value{
Un objeto \code{tagList} listo para ser incluido en una interfaz Shiny.
}
\description{
Genera un conjunto de botones radiales basados en \code{shinyWidgets::radioGroupButtons()}
con estilos personalizados y soporte para íconos y tooltips. Funciona tanto en
aplicaciones Shiny comunes como dentro de módulos utilizando un namespace opcional.
}
\examples{
BotonesRadiales(
  inputId = "estado",
  label = "Estado:",
  choices = c(Activo = "activo", Inactivo = "inactivo"),
  selected = "activo"
)
}
